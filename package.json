{
  "name": "phrase-sync",
  "version": "1.0.0",
  "description": "Phrase sync github action",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "prepare": "ncc build index.js -o dist --source-map --license licenses.txt",
    "test": "jest",
    "all": "yarn run lint && yarn run prepare && yarn run test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/getPopsure/phrase-sync.git"
  },
  "keywords": [
    "GitHub",
    "Actions",
    "JavaScript",
    "Phrase"
  ],
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1",
    "eslint": "^8.38.0",
    "jest": "^29.5.0"
  }
}
